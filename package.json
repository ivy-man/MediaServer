{
  "name": "media-server",
  "version": "0.0.1",
  "description": "Media server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "EliKa HM",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.0.0",
    "fastify": "^2.4.1"
  }
}
